package main

func Add(x int, y int) int {
	return x + y
}

func Abs(x int) int {
	if x < 0 {
		return -x
	}
	return x
}

func Max(a int, b int) int {
	if a > b {
		return a
	}
	return b
}

func Sign(x int) int {
	switch {
	case x < 0:
		return -1
	case x > 0:
		return 1
	default:
		return 0
	}
}
